# I am a C-Shell -* shell-script -*-

# Example "local" script: all scripts that end in '_example.csh' in
# this directory are ignored via .gitignore.

# This script doesn't do anything, (except consume CPU every time csh
# is run).

# end: example_local.csh
